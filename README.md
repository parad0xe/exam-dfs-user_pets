```

## Installation

Les fichiers du projets doivent être placé à la racine 
du serveur web pour fonctionner.

## Requirements

* PHP 7.0 minimum

## Base de donnée

Si les identifiants de connexion à la base de donnée sont valide
et que l'utilisateur a le droit de création de base de donnée, alors
cette dernière sera automatiquement crée avec un utilsateur par défaut au premier
lancement de l'application.

> Utilisateur par défaut:
> email: demo@demo.com  
> pass: demo

Autrement, un fichier au format SQL contenant l'intégralité des données 
requises est également joint.

```
